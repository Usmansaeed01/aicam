name: Build Android Native APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest
    
    # 🎯 TARGET FIX: Server ko mobile folder ke andar bhej diya
    defaults:
      run:
        working-directory: ./artifacts/mobile

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 📦 Install Dependencies
        run: npm install

      - name: 🏗️ Generate Native Code (Prebuild)
        run: npx expo prebuild --platform android --clean

      - name: ☕ Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: 🛠️ Build APK (Gradle)
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease

      - name: 🚀 Upload Final APK
        uses: actions/upload-artifact@v4
        with:
          name: AICam-Build
          # 🎯 PATH FIX: APK ka poora rasta artifacts se shuru hoga
          path: artifacts/mobile/android/app/build/outputs/apk/release/app-release.apk
